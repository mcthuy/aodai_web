<div class="login">
  <h2>Log in</h2>
  
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field">
      <%= f.label :email ,class: "email" %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email" , class: "input-textbox" %>
    </div>
  <br />
    <div class="field">
      <%= f.label :password , class: "password-L" %><br />
      <%= f.password_field :password, autocomplete: "current-password" , class: "input-textbox" %>
    </div>
  <br />
    <% if devise_mapping.rememberable? %>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me , class: "remember_me" %>
      </div>
    <% end %>
  <br />
    <div class="actions">
      <%= f.submit "Log in" , class: "bt-signin" %>
    </div>
  <% end %>
  <br />
  <%= render "devise/shared/links" %>
</div>
